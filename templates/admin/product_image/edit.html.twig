{% extends 'base.html.twig' %}

{% block title %}Editar Imagem do produto{% endblock %}

{% block body %}
    <h1>{{ product.title }}</h1>
    <h4>Editar Imagem do produto</h4>

    {{ include('admin/product_image/_form.html.twig', {'button_label': 'Salvar'}) }}

    <a href="{{ path('app_admin_product_image_index', {'productId': product.id}) }}" class="btn btn-outline-secondary me-2 float-start">Voltar</a>

    {{ include('admin/product_image/_delete_form.html.twig') }}
{% endblock %}
