{% extends 'base.html.twig' %}

{% block title %}Vizualizar Propriedade de Produtos{% endblock %}

{% block body %}
    <h2>Vizualizar Propriedade de Produtos</h2>

    <table class="table">
        <tbody>
            <tr >
                <th>Title</th>

                <td>{{ product_property.title }}</td>
            </tr>
        </tbody>
    </table>


    <a href="{{ path('app_admin_product_property_edit', {'id': product_property.id}) }}" class="btn btn-outline-info float-start me-2">Editar</a>

    <a href="{{ path('app_admin_product_property_index') }}" class="btn btn-outline-secondary me-2 float-start">Voltar</a>

    {{ include('admin/product_property/_delete_form.html.twig') }}


{% endblock %}
