<form method="post" action="{{ path('app_admin_product_property_delete', {'id': product_property.id}) }}" onsubmit="return confirm('Tem certeza que quer apagar este item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product_property.id) }}">
    <button class="btn btn-outline-danger ">Apagar</button>
</form>
